- name: Install nginx applitions
    apt:
      name: nginx
      state: latest
    become: true

- name: copy html to the nginx
  copy:
    src: index.html
    dest: /var/www/html/index.html
    owner: root
    group: root
    mode: 0644